namespace TestRunner;

public class TestRunner
{
    /// <summary />
    /// <param name="repo">path to the repository this will observe</param>
    /// <param name="dispatchServer">dispatcher host:port</param>
    /// <param name="host">runner's host</param>
    /// <param name="port">runner's port</param>
    public static async Task Main(string repo, string dispatchServer = "localhost:8888", string host = "localhost", int port = 8900)
    {
    }
}