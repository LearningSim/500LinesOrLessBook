<UserControl x:Class="Blockcode.Block"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             FontSize="20"
             Margin="0, 4"
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <TextBlock Margin="0,0,0,0" Text="{Binding Label, StringFormat= '\{0\} '}" />
            <TextBox Width="50" Text="{Binding Value}" Visibility="{Binding ValueVisibility}" />
            <TextBlock Margin="0,0,0,0" Text="{Binding Units, StringFormat= ' \{0\}'}" />
        </StackPanel>
        <StackPanel x:Name="ChildrenContainer" />
    </StackPanel>
</UserControl>